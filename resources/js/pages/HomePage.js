import React, { Fragment } from 'react';
import Header from '../components/Header';

const HomePage = () => {
    return (
        <Fragment>
            <Header></Header>
            <div>
                <h1>Homepage</h1>
            </div>
        </Fragment>
    );
};

export default HomePage;
